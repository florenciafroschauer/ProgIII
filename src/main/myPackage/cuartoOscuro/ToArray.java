package myPackage.cuartoOscuro;

public class ToArray {
    /**
    private void sortArray(UNARREGLO[] a) {
        for (int i = 0; i < a.length - 1; i++) {
            int pos = i;

            for (int j = i + 1; j < a.length-1; j++) {
                if (a[j].compareTo(a[pos])>0) { TIENE QUE IMPLEMENTAR COMPARABLE
                    pos = j;
                }
            }
            AttendedPatient smallerWaitTime = a[pos];
            a[pos] = a[i];
            a[i] = smallerWaitTime;
        }
    }

    private void toArray(LISTADINAMICA patientsAttended1, ARRAY patientsAttended2){
        for (int i = 0; i <patientsAttended2.length ; i++) {
            patientsAttended1.goTo(i);
            patientsAttended2[i] = patientsAttended1.getActual();
        }
    }
     */
}
